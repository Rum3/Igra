package noirdetective.game.domain.usecase

class CheckHiddenEndingUseCase {
    operator fun invoke(): Boolean {
        // Logic to check if all side stories are completed correctly
        return false
    }
}
